<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zoo Management</title>
    <!-- Add Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4"><%= animal ? 'Edit Animal' : 'Add Animal' %></h1>

        <!-- Form for Add/Edit Animal -->
        <form action="<%= animal ? '/edit/' + animal.id : '/add' %>" method="POST">
            <div class="mb-3">
                <label for="name" class="form-label">Animal Name</label>
                <input type="text" id="name" name="name" class="form-control" value="<%= animal ? animal.name : '' %>" required>
            </div>
            <div class="mb-3">
                <label for="species" class="form-label">Species</label>
                <input type="text" id="species" name="species" class="form-control" value="<%= animal ? animal.species : '' %>" required>
            </div>
            <div class="mb-3">
                <label for="age" class="form-label">Age</label>
                <input type="number" id="age" name="age" class="form-control" value="<%= animal ? animal.age : '' %>" required>
            </div>
            
            <button type="submit" class="btn btn-success w-100">Save Animal</button>
        </form>

        <div class="mt-3">
            <a href="/" class="btn btn-secondary w-100">Back to Animal List</a>
        </div>
    </div>

    <!-- Bootstrap JS (Optional for interactivity, like modals or dropdowns) -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>
